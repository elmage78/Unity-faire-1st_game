using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScoreXtra : MonoBehaviour
{
    MasitAI MasitAI;
    // Start is called before the first frame update
    void Start()
    {
        MasitAI = GetComponent<MasitAI>();
    }

    // Update is called once per frame
    void Update()
    {
    }
}
